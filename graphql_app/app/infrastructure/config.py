import os

class Settings:
    PROJECT_NAME: str = "Catálogo de Productos GraphQL (Clean Architecture)"
    PROJECT_VERSION: str = "1.0.0"
    HOST: str = "127.0.0.1"
    PORT: int = 8000

settings = Settings()
